% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessor.R
\name{create_regular_episodes_df}
\alias{create_regular_episodes_df}
\title{Create Processed Data Frame for Regular Episodes}
\usage{
create_regular_episodes_df(
  raw_df,
  add_q_and_a = TRUE,
  add_difficulty = TRUE,
  viewer_assumptions = FALSE,
  filter_notes = TRUE
)
}
\arguments{
\item{raw_df}{The raw data frame (tibble) read from the source TSV.}

\item{add_q_and_a}{Logical, if TRUE adds the 'question_and_answer' column.}

\item{add_difficulty}{Logical, if TRUE adds the 'clue_difficulty' column.}

\item{viewer_assumptions}{Passed to `add_clue_difficulty` if `add_difficulty` is TRUE.}

\item{filter_notes}{Logical, if TRUE removes rows where the 'notes' column is not NA (default TRUE).}
}
\value{
A tibble containing the processed data for regular episodes.
}
\description{
Filters the raw Jeopardy data for standard Jeopardy! and Double Jeopardy!
rounds, cleans column names, optionally adds a combined Question/Answer
column, and optionally adds a clue difficulty classification.
}
\examples{
# Assuming jeopardy_raw is your loaded raw data:
# regular_data <- create_regular_episodes_df(jeopardy_raw)
# head(regular_data)
}
